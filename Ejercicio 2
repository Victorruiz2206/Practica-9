Utilizando una lista capturar las 5 calificasiones de un alumno e imprimir el promedio.
#include <stdio.h>

void main ()
{
    int list[5];
    int i, calf = 0; 
    float promedio;
    
    for (i=0; i<5; i++)
    {
        printf("Introduzca las calificasiones %d: ", i +1);
        scanf( "%d", &list[i]);
    }
    for (i=0; i<5; i++)
    {
        calf= calf + list[i];
    }
    promedio= calf / 5;
    printf("El promedio es: %f\n", promedio);
}
